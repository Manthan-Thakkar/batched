apiVersion: v1
kind: Secret
metadata:
  name: grafana-smtp-creds
type: Opaque
stringData:
  user: notifications@amtechsoftware.com
  # NOTE: Do not commit real SMTP passwords to source control.
  # The actual password must be provided securely (e.g., via an external
  # secret manager or injected at deploy time by the CI/CD pipeline).
  password: ""
